package lambda;
@FunctionalInterface
public interface Test {
    void display();
    /*
    1. check if interface has only one abstract method
    2. check if parameter of lambda same as interface method
    3. check if return value of lambda same as interface method return type
     */
}
